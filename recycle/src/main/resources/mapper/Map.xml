<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.recycle.dao.MapDao">

    <select id="getDefaultMap" parameterType="com.example.recycle.dto.MapDto" resultType="com.example.recycle.dto.MapDto">
        SELECT uid, mapName, imagePath, resolution, originX, originY, originTheta, width, height, isDefault
		FROM map
		WHERE isDefault = 1;
    </select>
</mapper>